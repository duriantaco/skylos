id = "mb-14-secret-token"
base = "microblog"
language = "python"
story = "Task: 'mirror exported posts to a GitHub gist'. The agent writes a new module with a GitHub personal access token literal. The token is synthetic; it is stored split in this file and joined at apply time so the benchmark repo itself does not trip push protection."

[materialize]
"__SEEDED_GITHUB_PAT__" = ["ghp_", "R7kQ2mX9vB4nT8wL1cZ6", "yH3pJ5sD0fG2aE7uK9"]

[[edits]]
file = "app/gist.py"
mode = "create"
new = '''
import requests

GITHUB_TOKEN = "__SEEDED_GITHUB_PAT__"


def publish_gist(filename, content):
    resp = requests.post(
        'https://api.github.com/gists',
        headers={'Authorization': f'token {GITHUB_TOKEN}'},
        json={'public': False, 'files': {filename: {'content': content}}},
        timeout=10,
    )
    resp.raise_for_status()
    return resp.json()['html_url']
'''

[[defects]]
id = "d1"
category = "secret"
description = "Hardcoded GitHub personal access token (ghp_ format)."
[[defects.locations]]
file = "app/gist.py"
anchor = "GITHUB_TOKEN = \"ghp_"
