{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.duriantaco/skylos",
  "description": "MCP server for AI agents to run open-source code security and static analysis on Python, TypeScript, and Go. Detects dead code, secrets, vulnerabilities, and diff-aware security regressions.",
  "repository": {
    "url": "https://github.com/duriantaco/skylos",
    "source": "github"
  },
  "version": "4.2.1",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "skylos",
      "version": "4.2.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Skylos API key. Required for every MCP tool except `analyze` (which works without a key, 5 calls/day). The local CLI (`skylos verify`, hooks) needs no key.",
          "isRequired": false,
          "format": "string",
          "isSecret": true,
          "name": "SKYLOS_API_KEY"
        }
      ]
    }
  ]
}
