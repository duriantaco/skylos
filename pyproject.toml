[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "skylos"
version = "0.1.0"
requires-python = ">=3.8"

[project.scripts]
skylos = "skylos.cli:main"

[tool.maturin]
module-name = "skylos._core"
python-source = "."
